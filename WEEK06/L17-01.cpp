#include <iostream>
using namespace std;
// POINTER

int main()
{
    // int a = 5;
    // int *ptr = &a;

    // // this will print the address of "a"
    // cout << "The address of 'A' stored in ptr is : " << ptr << endl;

    // // DEREFRENCE OPERATOR --> this will print the value stored at the address stored in "ptr" pointer
    // cout << "The value stored at the address stored in 'ptr' pointer : " << *ptr << endl;

    // // This will print the address of "ptr" memory block
    // cout<<"Address of 'ptr' is : "<<ptr<<endl;

    // int arr[5]={1,2,3,4,5};
    // arr=arr+1; //this will throw an error as we can't change the base address of an Array

    // char ch = 'a';
    // char* cptr =&ch;
    // cout<<cptr<<endl;

    // char ch[10] = "a";
    // char* cptr =&ch; 
    // cout<<cptr<<endl;   //this will throw error cause &ch will provide the address of entire char array instead of base address

    return 0;
}